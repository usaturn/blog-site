[project]
name = "blog-site"
version = "0.1.0"
description = "usaturn の個人ブログ"
requires-python = ">=3.14"
dependencies = [
    "maatlog>=0.4.0",
]

[dependency-groups]
dev = [
    "sphinx-autobuild",
]

[tool.uv]
package = false
